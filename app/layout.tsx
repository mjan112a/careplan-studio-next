import type React from "react"
import { Inter } from "next/font/google"
import "./globals.css"
import { ThemeProvider } from "@/components/theme-provider"

const inter = Inter({ subsets: ["latin"] })

export const metadata = {
  title: "CarePlan Studio - Transform LTC Insurance Illustrations Into Impact",
  description:
    "Upload any LTC policy, simulate outcomes, and show clients exactly how insurance protects their future.",
  keywords: "LTC insurance, long-term care, financial advisor tools, insurance illustration, retirement planning",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <ThemeProvider attribute="class" defaultTheme="light" enableSystem disableTransitionOnChange>
          {children}
        </ThemeProvider>
      </body>
    </html>
  )
}



import './globals.css'